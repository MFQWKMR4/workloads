steps:
  - id: go-contention
    runtime: golang
    location: runtimes/golang/main.go
    parallel:
      processes: 1
    duration_ms: 100000
    wrapper: "strace -f -c"
    env:
      WL_WORKERS: "16"
      WL_HOLD_US: "50"
